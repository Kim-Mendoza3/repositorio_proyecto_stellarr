# Netlify Configuration for Next.js Frontend
# Simple configuration for Next.js deployment

[build]
  command = "npm ci && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"

# Routes for Next.js SPA
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
